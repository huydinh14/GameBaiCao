<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity_TimePlay"
    android:paddingTop="10dp"
    android:paddingLeft="40dp"
    android:paddingRight="40dp"
    android:paddingBottom="40dp"
    android:orientation="vertical"
    android:background="@drawable/background">
    <ImageView
        android:id="@+id/imageBackTimeMenu"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/back_to_64px"/>
        <EditText
            android:id="@+id/editText_TimePlay"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:textColorHint="@color/white"
            android:background="@drawable/boder_bottom"
            android:hint="Nhập thời gian chơi (giây)"
            android:layout_marginTop="200dp"
            android:inputType="number"
            android:focusableInTouchMode="true"
            android:gravity="center"/>
    <requestFocus/>
    <Button
        android:id="@+id/button_BatDauGame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="BẮT ĐẦU"
        android:backgroundTint="#004d99"/>
</LinearLayout>